version: '3.8'
services:
  node:
    container_name: ${PROJECT_NAME}
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - ${SERVER_PORT}
    ports:
      - ${SERVER_PORT}:${SERVER_PORT}
    networks:
      - intranet
      - container

networks:
  container:
    driver: bridge
  intranet:
    external: true
